# Video Moments - Web

Minimal Vite React app that mimics the phone mock from the React Native app.

How to run

Install dependencies:

```bash
cd video-moments-app-website
npm install
npm run dev
```

Open the local dev URL shown by Vite and upload a video to preview it in the phone mock.
